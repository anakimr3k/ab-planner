import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Tu peux ajouter des options ici plus tard si besoin */
  /* Mais pour l'instant, on laisse vide pour garantir le succès du build */
};

export default nextConfig;
